# ==============================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================
# This is a template for setting up your .env file
# Copy this file to .env and fill in your actual values
#
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Generate a JWT secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Replace the placeholder values below with your actual configuration
# 4. NEVER commit .env to git (it's already in .gitignore)
# ==============================================

# ---------------------------------------------
# DATABASE CONFIGURATION
# ---------------------------------------------
# PostgreSQL connection string format:
# postgresql://username:password@host:port/database
#
# For local development, the default values work with docker-compose:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/billings

# For production, use a dedicated database user:
# DATABASE_URL=postgresql://billings_user:STRONG_PASSWORD@db.example.com:5432/billings_prod

# ---------------------------------------------
# JWT AUTHENTICATION CONFIGURATION
# ---------------------------------------------
# Secret key for signing JWT tokens
# CRITICAL: Generate a unique secret for each environment!
# Never use the same secret in dev and production!
#
# Generate a new secret:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-generated-secret-key-here-minimum-32-characters

# JWT token expiration time in minutes
# Development: 15-60 minutes is reasonable
# Production: 5-15 minutes with refresh token strategy
JWT_EXPIRATION_MINUTES=15

# ---------------------------------------------
# DOCKER POSTGRES CONFIGURATION
# ---------------------------------------------
# These are used by docker-compose.yml for the PostgreSQL container
# For development, defaults are fine
# For production, use strong passwords!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=billings

# ---------------------------------------------
# PGADMIN CONFIGURATION
# ---------------------------------------------
# Web-based PostgreSQL admin tool credentials
# Access at: http://localhost:5050 after running docker-compose up
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# ==============================================
# SECURITY NOTES FOR LEARNING:
# ==============================================
#
# 1. NEVER commit .env files to git
#    - They contain secrets and credentials
#    - .env is already in .gitignore
#
# 2. Use different secrets for different environments
#    - Development: Can use simpler values for learning
#    - Production: MUST use cryptographically secure random secrets
#
# 3. JWT_SECRET is critical for security
#    - Anyone with this secret can forge authentication tokens
#    - If compromised, all existing tokens are invalid
#    - Rotate this secret periodically in production
#
# 4. Database credentials
#    - Development: postgres/postgres is fine for learning
#    - Production: Create dedicated user with limited permissions
#
# 5. Keep this .env.example file updated
#    - When adding new environment variables, update this template
#    - This helps other developers (or future you) set up the project
# ==============================================
